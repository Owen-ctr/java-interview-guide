# Java 集合

<Badge type="info" text="模块：Java 集合" />

本模块覆盖 `Map`、`List`、`Set` 及迭代器等面试常考点：

- [Map 集合](./map) — `HashMap` 与 `Hashtable`、`HashMap` 底层原理、`ConcurrentHashMap`
- [List / Set 与迭代](./list-set-iterator) — `ArrayList` 与 `LinkedList`、`HashSet`、`fail-fast` 与 `fail-safe`

> 难度分级：**基础 / 进阶 / 深入**。
